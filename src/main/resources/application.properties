# =======================
# Application Information
# =======================
spring.application.name=Attendance

# =======================
# Server Configuration
# =======================
server.port=${SERVER_PORT:5050}

# =======================
# MongoDB Configuration
# =======================
# Use a single URI property; avoid hardcoded credentials
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://agarwalshubh1972004:Shubh123@cluster0.0xmfhf7.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0}
spring.data.mongodb.database=${MONGO_DB:attendanceDB}
spring.data.mongodb.auto-index-creation=true

# =======================
# JWT Configuration
# =======================
# ADD JWT SECRET KEY HERE (512 bits)
app.jwt.secret=${JWT_SECRET}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# =======================
# Email Configuration
# =======================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
# ADD SMTP MAIL ID HERE
spring.mail.username=${MAIL_ID_SMTP}
# ADD SMTP PASSWORD HERE
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# =======================
# HR Configuration
# =======================
# ADD HR DETAILS HERE
app.hr.email=${HR_EMAIL}
app.hr.name=${HR_NAME}
app.hr.contact=${HR_CONTACT}

# =======================
# Logging
# =======================
logging.level.com.company.amsbackend=DEBUG
logging.level.org.springframework.security=DEBUG

# =======================
# Actuator Configuration
# =======================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when_authorized

# Set timezone for Jackson date serialization
spring.jackson.time-zone=Asia/Kolkata
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss